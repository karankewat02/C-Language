#include <stdio.h>

int main()
{
	char a,b,c,d;
	int x,y;
	printf("Enter the name : ");
	scanf("%s",a);
	printf("Enter the name : ");
	scanf("%s",b);
	printf("Enter the name : ");
	scanf("%s",c);
	printf("Enter the name : ");
	scanf("%s",d);
	char arr[] = {a,b,c,d};
	for(x=)
	return 0;
}
